version: '3'
#MAINTAINER: max-30ru@mail.ru
services:
  db:
    image: postgres:12.2
    environment:
      POSTGRES_PASSWORD: test
      POSTGRES_user: test
    volumes:
      - ./data/:/var/lib/postgresql/data/
    ports:
      - 5432:5432



  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
